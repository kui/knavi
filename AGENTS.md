- Run `npm run fix && npm run lint && npm run test` **once** after all code changes are complete and before delivering the final response. Do not run it repeatedly during iterative work or before asking the user a question unless new changes were made since the last run.
- Write all PR, code, and commit comments in English.
- Use mise to manage tool versions. Ensure `mise install` is run before building.
