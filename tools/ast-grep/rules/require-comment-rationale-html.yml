id: require-comment-rationale-html
language: Html
severity: error
message: |
  Comment lacks a rationale keyword. Either delete it, or prefix it with
  WHY: / HACK: / SAFETY: / INVARIANT:.
rule:
  kind: comment
  not:
    any:
      - regex: '^<!--\s*(WHY|HACK|SAFETY|INVARIANT)\b'
      - regex: '^<!--\s*\[if '
